<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="App6.Page1">
    <ContentPage.Content>
        
            <StackLayout>
                <Label>Známka</Label>
                <Entry x:Name="znamka"></Entry>
                <Label>Váha</Label>
                <Entry x:Name="vaha"></Entry>
                <Label>id predmět</Label>
                <ListView x:Name="predment">
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <TextCell Text="{Binding Name}" />
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>

                <Button Clicked="Button_Clicked" Text="Přidat"></Button>
                <Label></Label>
                <Label>Přidat nový předmět</Label>
                <Entry x:Name="novypredmet"></Entry>
                <Button Clicked="Button_Clicked_1" Text="Přidat"></Button>
            </StackLayout>
        
    </ContentPage.Content>
</ContentPage>